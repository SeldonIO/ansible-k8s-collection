apiVersion: v1
kind: Secret
metadata:
  name: "{{ minio_rclone_secret_name }}"
type: Opaque
stringData:
  RCLONE_CONFIG_S3_TYPE: s3
  RCLONE_CONFIG_S3_PROVIDER: minio
  RCLONE_CONFIG_S3_ENV_AUTH: "false"
  RCLONE_CONFIG_S3_ACCESS_KEY_ID: "{{ minio_access_key }}"
  RCLONE_CONFIG_S3_SECRET_ACCESS_KEY: "{{ minio_secret_key }}"
  RCLONE_CONFIG_S3_ENDPOINT: "http://minio.{{ minio_namespace }}.svc.cluster.local:9000"
